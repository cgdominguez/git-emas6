const list = new Set();// set va con mayuscula, -Set sentencia /objeto, un recurso

list.add("item1");
list.add("item2").add("item3");// de esta manera encadenamos varios add  

console.log(list);// nos devuelve un objeto de estos
//Set(3) { 'item1', 'item2', 'item3' }
